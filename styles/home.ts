import styled from "styled-components";

export const Container = styled.main`
	display: flex;
	overflow-y: hidden;
	overflow-x: hidden;

	.divisor {
		display: flex;
		flex-direction: column;
	}

	> nav {
		display: flex;
		float: left;
	}
`
